# Private Repos

VibeRaven can be used from private repos through the CLI.

The CLI sends safe scan inputs to the hosted VibeRaven service after user login. Secrets should stay in environment variables or secret managers and should not be committed.

This public repository does not contain VibeRaven's private scanner internals, billing implementation, auth backend, provider intelligence, or VS Code extension source.
