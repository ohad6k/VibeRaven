description = "Separate repo-code fixes from provider dashboard or human actions"
prompt = "Use the provider-actions skill. Separate every item into repo-code fixes, provider dashboard actions, and evidence requests. For each provider action, name the provider, project/environment if known, dashboard path or setting, expected value or behavior, and acceptable evidence format. Do not ask for secrets. Return exactly four sections: evidence found, evidence missing, repo-code fixes or none, provider or human action needed."
