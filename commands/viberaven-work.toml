description = "Use VibeRaven on the current production-sensitive task"
prompt = "Use VibeRaven as a task workflow. Inspect what changed, read or propose .viberaven/production-context.md, identify release/version and provider risk, then make the smallest repo-code fix the evidence supports if the user asked for implementation. Separate provider or human actions from repo-code fixes. Do not ask for secrets."
