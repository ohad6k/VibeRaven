description = "Push and deploy with proof"
prompt = "Use the go-live skill. Push the app to GitHub safely, deploy to Vercel when configured, collect build and live URL proof, update production context, and separate any provider dashboard or human action that still needs proof. Do not include secrets, build output, or unrelated dirty work."
