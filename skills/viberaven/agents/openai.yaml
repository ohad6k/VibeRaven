interface:
  display_name: "VibeRaven — Control Layer for AI-Built Products"
  short_description: "Product context + launch verdict for coding agents, fully local"
  brand_color: "#ff4d1f"
  default_prompt: "Use $viberaven to map this product and clear launch blockers before shipping."

policy:
  allow_implicit_invocation: true
