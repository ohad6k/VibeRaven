{
  "name": "@viberaven/cli",
  "version": "1.0.5-public-localhost",
  "description": "Local-first VibeRaven public source CLI and localhost UI.",
  "license": "MIT",
  "bin": {
    "viberaven": "dist/cli.js"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "tsc -p tsconfig.json",
    "start": "node dist/cli.js",
    "scan": "node dist/cli.js scan",
    "verify": "node dist/cli.js --verify"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.8.0"
  }
}
