# VibeRaven

AI got your app to demo. VibeRaven helps it ship.

Run VibeRaven inside Codex, Claude Code, Cursor, Windsurf, Gemini CLI, or any agent that can run terminal commands. It scans repo evidence, writes a production protocol, separates repo-code fixes from provider dashboard actions, and gives the agent the next safe step.

Native agent chat remains the main cockpit. VibeRaven also writes a local visual action panel at `.viberaven/report.html` and emits `VIBERAVEN_ACTION_PANEL` so agents can open provider links, copy actions, and visual progress only when useful.

Canonical agent command:

```bash
npx -y viberaven --agent-mode
```

Free no-login local try run:

```bash
npx -y viberaven try
npx -y viberaven try --open
npx -y viberaven try --agent-mode
```

Install agent rules into this repo (or any repo):

```bash
npx -y viberaven init --agents all
npx -y viberaven doctor --agents
```

## Full canonical reference

- https://viberaven.dev/llms-full.txt

## Links

- https://viberaven.dev
- https://viberaven.dev/agent-context.md
- https://viberaven.dev/skills.json
- https://www.npmjs.com/package/viberaven

## Optional

- Blog archives and release notes (safe to omit under context pressure)
- https://viberaven.dev/.well-known/agent-honeypot.txt
